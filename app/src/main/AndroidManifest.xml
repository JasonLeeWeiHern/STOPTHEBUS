<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.stopthebus">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".busStopList"
            android:label="96"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".Destination"
            android:label="Alarm"/>
        <activity android:name=".Help"
            android:label="Contact Us"/>
        <activity android:name=".NearbyStops"
            android:label="BusStops Near You"/>
        <activity android:name=".AllBusesNumber"
            android:label="Buses"/>
        <activity android:name=".bus_95"
            android:label="95"/>
        <activity android:name=".bus_A1"
            android:label="A1"/>
        <activity android:name=".bus_A2"
            android:label="A2"/>
        <activity android:name=".bus_B1"
            android:label="B1"/>
        <activity android:name=".bus_B2"
            android:label="B2"/>
        <activity android:name=".bus_btc1"
            android:label="BTC1"/>
        <activity android:name=".bus_C"
            android:label="C"/>
        <activity android:name=".bus_D1"
            android:label="D1"/>
        <activity android:name=".bus_D2"
            android:label="D2"/>
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>